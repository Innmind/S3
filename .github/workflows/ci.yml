name: Continuous Integration
on: [push]
jobs:
    phpunit:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@master
            -
                name: PHPEnv
                run: 'ls -l /etc/alternatives | grep php'
